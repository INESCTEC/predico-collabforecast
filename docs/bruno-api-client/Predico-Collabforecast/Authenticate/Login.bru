meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: https://{{PredicoBaseUrl}}/api/v1/token
  body: json
  auth: none
}

body:json {
  {
    "email": "string",
    "password": "string"
  }
}

script:post-response {
  let jsonData = res.getBody();
  bru.setEnvVar("BearerToken",jsonData.access);
}
